#! /usr/bin/env python

from hlt import *


i=0
while ( i<100 ):
    tname="Trig" + `i`
    pscale=i*3
    trig( tname, pscale)
    i=i+1

#Make the conf files for this trigger table
make_cfg_file('triggers.cfi')
 
#Now cmsRun
import os
os.system("pwd")
#os.system("cmsRun hlt.cfg");

