import FWCore.ParameterSet.Config as cms

# Standard Iterative Cone Jets parameters
# $Id
IconeJetParameters = cms.PSet(
    seedThreshold = cms.double(1.0),
    debugLevel = cms.untracked.int32(0)
)

