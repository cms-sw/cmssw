// ShR 17 Sept 2005: add this until scram can handle packages with only templated classes
namespace {
  namespace {
    class dummyfile {
     public:
      dummyfile() { };
      virtual ~dummyfile() { };
    };
  }
}
