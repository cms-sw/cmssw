#include "CondFormats/Common/interface/ClassIDCapability.h"
#include "../src/classes.h"

/*
namespace {
  cond::ClassID<Pedestals> aid;
  cond::ClassID<BlobPedestals> bid;
  cond::ClassID<BlobNoises> cid;
}
*/

CLASS_ID(Pedestals, aid);
CLASS_ID(BlobPedestals, bid);
CLASS_ID(BlobNoises, cid);

