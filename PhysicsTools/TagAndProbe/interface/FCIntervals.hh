#ifndef __FC_INTERVALS_H__
#define __FC_INTERVALS_H__

namespace  FCIntervals {
  void Efficiency( const double pass, const double total,
		   double& mode, double& lowErr, double& highErr);
}

#endif
