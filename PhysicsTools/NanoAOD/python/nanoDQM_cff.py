# automatically generated by prepareDQM.py
import FWCore.ParameterSet.Config as cms
from PhysicsTools.NanoAOD.nanoDQM_tools_cff import *

nanoDQM = cms.EDAnalyzer("NanoAODDQM",
    vplots = cms.PSet(
        Electron = cms.PSet(
            plots = cms.VPSet(
                Plot1D('pt', 'pt', 20, 0, 200),
            )
        ),
        Jet = cms.PSet(
            plots = cms.VPSet(
                Plot1D('pt', 'pt', 20, 0, 400),
            )
        ),
        MET = cms.PSet(
            plots = cms.VPSet(
                Plot1D('pt', 'pt', 20, 0, 400),
            )
        ),
        Muon = cms.PSet(
            plots = cms.VPSet(
                Profile1D('mediumId', 'mediumId', 'pt', 16, 0, 80),
                Plot1D('pt', 'pt', 20, 0, 200),
            )
        ),
        Photon = cms.PSet(
            plots = cms.VPSet(
                Plot1D('pt', 'pt', 20, 0, 200),
            )
        ),
        Tau = cms.PSet(
            plots = cms.VPSet(
                Plot1D('pt', 'pt', 20, 0, 200),
            )
        ),
    )
)
