Utilities to convert a CSV (Comma Separated Value) file to a MySQL DB and to read it and dump a dummy output

convertOptAlign.cfg

Converts the file 

opt_align_simple2DWithMirror.csv

into a DB, that can be later used as input for the COCOA description file Alignment/CocoaApplication/bin/simple2DWithMirror.txt

