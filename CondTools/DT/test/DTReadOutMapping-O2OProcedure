####This is a simulation of O2O procedure for DTReadOutMapping object####
    
step 1: prepare offline db schema(this is a simulation of ORCON/ORCOFF DBA)

 sqlplus cms_xiezhen_dev@devdb10/xiezhen123<DT-payload.sql
 sqlplus cms_xiezhen_dev@devdb10/xiezhen123<CondIOV.sql
 sqlplus cms_xiezhen_dev@devdb10/xiezhen123<createMetaDataTable.sql

step 2: fill dummy data in offline payload tables with two sets of data 
        in cms O2O, these tables are filled by O2O queries(this is a 
simulation of online to offline transformation)
 sqlplus cms_xiezhen_dev@devdb10/xiezhen123<DT-fillmapping.sql
 
step 3: setup and fill pool tables(this is a simulation of "poolification" steps after the transformation)
   #setenv CORAL_AUTH_USER cms_xiezhen_dev not needed if using -u -p
   #setenv CORAL_AUTH_PASSWORD xiezhen123
   ../../Utilities/bin/setup_pool_database DTReadOutMapping CondFormatsDTObjects oracle://devdb10/cms_xiezhen_dev ../src/DTReadOutMapping-mapping-custom.xml -u cms_xiezhen_dev -p xiezhen123
   ../../Utilities/bin/setup_pool_database "cond::IOV" CondCoreIOVService oracle://devdb10/cms_xiezhen_dev CondIOV-mapping-custom.xml  (will be removed in the future)

step 4: FCregisterPFN -p oracle://devdb10/cms_xiezhen_dev -t POOL_RDBMS(only if the first time)

step 5: build and tag IOV. NOTE: two infinite IOV tagged by different 
tags. The selection of the data is by query on the extra columns in table 
DTREADOUTMAPPING (this is a simulation of tagging the data from online)

   cmscond_build_iov_byquery -i -q "CELL_MAP_VERSION='my_ROB' AND ROB_MAP_VERSION='my_ROS'" -u cms_xiezhen_dev -p xiezhen123 -d CondFormatsDTObjects -t DTREADOUTMAPPING -o DTReadOutMapping -c oracle://devdb10/cms_xiezhen_dev myversion
   cmscond_build_iov_byquery -i -q "CELL_MAP_VERSION='cmssw_ROB' AND ROB_MAP_VERSION='cmssw_ROS'" -u cms_xiezhen_dev -p xiezhen123 -d CondFormatsDTObjects -t DTREADOUTMAPPING -o DTReadOutMapping -c oracle://devdb10/cms_xiezhen_dev cmssw_version

step 6: read data back from cmsRun. Note the tag value (this is a simulation of offline CMSSW job)
   cmsRun -p load_test_myversion.cfg
   cmsRun -p load_test_cmsswversion.cfg
