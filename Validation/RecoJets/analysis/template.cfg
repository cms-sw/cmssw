process VALIDATION = {
  untracked PSet maxEvents = {untracked int32 input = -1}
  service = DaqMonitorROOTBackEnd{ }
  source = PoolSource {
     untracked vstring fileNames = {INPUT_FILES}
  }

  module JetAnalyzer = CaloJetTester{
     InputTag src = SOURCE
     untracked string outputFile = 'OUTPUT_FILE'
  }
  path p = { JetAnalyzer }

}
