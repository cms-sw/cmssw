 - fix / add boundaries for all (existing combination of) bounded surfaces
 - add boundaries to material-by-layer plots
 - add eta to global RZ plots
 - add option of log material scale in RZ plots
 - add (global and) local eta-phi or Z-phi (barrel) + R-phi (forward) plots (lego or grayscale, possibly in log scale)

 - compute material by
   - layer
   - group of detectors (this will go in the XML)
   - subgroup (phi rings) (to check for meaningful grouping)
   - R/Z/eta/phi profiles
 - wishlist (1): be able to plot on a per layer basis:
    - material profle (vs. Z or eta or R)
    - material by phi ring (subgroup ?)
    - material by group
    - material by layer
   using (approximatly) correct Z/eta/R ranges
 - whishlist (2): what about checking for actual phi simmetry ?
