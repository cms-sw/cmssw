
      SUBROUTINE FIOOPN(UNIT, EVFILE)
      INTEGER UNIT
      CHARACTER*(*) EVFILE

      OPEN(UNIT, FILE=EVFILE, STATUS='UNKNOWN')

      END

      SUBROUTINE FIOCLS(UNIT)
      INTEGER UNIT

      CLOSE(UNIT)

      END
